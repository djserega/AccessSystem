<Page x:Class="AccessSystem.Forms.FormRequest.List"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AccessSystem.Forms.FormRequest"
      mc:Ignorable="d" x:Name="ListRequest"
      d:DesignHeight="300" d:DesignWidth="300"
      Title="Заявки">
    <Grid>
        <StackPanel>
            <Menu Style="{StaticResource Menu}">
                <MenuItem Header="Создать"  Style="{StaticResource MenuItem}"/>
                <Separator Width="3"/>
                <MenuItem Header="Редактировать" Style="{StaticResource MenuItem}"/>
            </Menu>
            <Grid Margin="5,0,5,0">
                <DataGrid />
            </Grid>
        </StackPanel>
    </Grid>
</Page>
