<Page x:Class="AccessSystem.Forms.FormBase.List"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AccessSystem.Forms.FormBase"
      xmlns:as="clr-namespace:AccessSystem"
      mc:Ignorable="d" x:Name="ListBase"
      d:DesignHeight="300" d:DesignWidth="650"
      Title="Базы данных">

    <Grid>
        <StackPanel>
            <Menu Style="{StaticResource Menu}">
                <MenuItem x:Name="ButtonCreate" Style="{StaticResource MenuItemCreate}"
                          Click="ButtonCreate_Click"/>
                <Separator Width="3"/>
                <MenuItem x:Name="ButtonEdit" Style="{StaticResource MenuItemEdit}"
                          Click="ButtonEdit_Click"/>
            </Menu>
            <Grid x:Name="Grid" Style="{StaticResource GridDataGridBase}">
                <DataGrid x:Name="DataGridList" Style="{StaticResource DataGridBase}"/>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
